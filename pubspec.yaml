name: student_application
description: "This is the frontend desktop app for the project 'Digital Exam Surveillance'"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  local_notifier: ^0.1.5
  window_manager: ^0.3.8
  ffi: ^2.1.0
  http: ^1.2.0
  system_tray: ^2.0.3


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

  assets:
    - assets/
